# Aegis AI — Environment Configuration
# Copy this file to .env and fill in your values

# API base URL (default: https://api.aegisai.io/v1)
VITE_API_BASE_URL=https://api.aegisai.io/v1

# Optional: Override default language (default: en)
# VITE_DEFAULT_LANG=en
